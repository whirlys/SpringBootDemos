spring:
  datasource:
    url: jdbc:mysql://localhost:3306/springbootdb?characterEncoding=utf8
    username: root
    password: whirly123
  jpa:
    database: MYSQL
    show-sql: true
  #Hibernate ddl auto (validate|create|create-drop|update)
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect